name: showcase
version: 1.1.0

authors:
  - Igor Borges <igor@borges.me>

description: Generate documentation screenshots of your widgets by just annotating them.
homepage: https://github.com/Igor1201/showcase

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  build: ^1.2.2
  source_gen: ^0.9.4+7
  analyzer: ^0.39.4
  glob: ^1.2.0
  http: ^0.12.0+4
  yaml: ^2.2.0
  dart_style: ^1.3.3

dev_dependencies:
  test: ^1.9.4
  pedantic: ^1.8.0
