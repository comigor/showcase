name: showcase
version: 0.3.1

authors:
  - Igor Borges <igor@borges.me>

description: Generate documentation screenshots of your widgets.
homepage: https://github.com/Igor1201/showcase

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  build: '>=0.12.6 <2.0.0'
  source_gen: ^0.9.0
  analyzer: ^0.36.0
  glob: ^1.1.7
  http: ^0.12.0+1

dev_dependencies:
  test: ^1.0.0
